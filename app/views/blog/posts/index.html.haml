%section#content.col-md-8
  %header.title
    %h1= t '.title'
    = create_button Blog::Post
  = render @posts
  = paginate @posts

.sidebar.sidebar-right.col-md-4
  = auto_discovery_link_tag :atom, posts_path(:atom)
  = render_cell 'blog/category', :show, view: :accordion_menu